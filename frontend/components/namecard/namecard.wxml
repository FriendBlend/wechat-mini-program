<view class="namecard external-class" style="background: {{skin}}; box-shadow: {{hasShadow ? '0px 4px 4px 0px rgba(0, 0, 0, 0.25)' : 'none'}};">
  <view class="left-side">
    <image class="avatar" src="{{avatarUrl}}" mode="aspectFill"></image>
    <text class="name">{{name}}</text>
  </view>
  <view class="right-side">
    <image class="status-icon" wx:if="{{status != null && status != 'joined'}}" src="../../images/namecard/status-{{status}}.png"/>
  </view>
  <slot></slot>
</view>